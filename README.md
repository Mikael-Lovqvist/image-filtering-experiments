# Current purpose
A pipeline of image operations that is useful when post processing stable diffusion output into playable worms armageddon levels.

# Installation
1. Clone repository
2. Install dependencies
  ```console
  $ npm install
  ```

# Usage
```console
$ node worm-pipeline.mjs input.png output.png
```
